# Images

Empty on purpose. The experience carries no photographic content — the résumé
data in `src/data/` is the only source of truth for what appears on screen.

If a project screenshot or a profile photograph is added later, put it here
and reference it from the matching entry in `src/data/`, not from a component.
